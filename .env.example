EURO_SYZYGY_DEV_COOKIE=

# Optional OpenSearch persistence sink. When BENCH_OPENSEARCH_URL is unset,
# runs are written only to bench-results/.
BENCH_OPENSEARCH_URL=
BENCH_OPENSEARCH_INDEX=cwv-bench-runs
BENCH_OPENSEARCH_USERNAME=
BENCH_OPENSEARCH_PASSWORD=
BENCH_OPENSEARCH_INSECURE_TLS=
